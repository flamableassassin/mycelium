const Embed = require('../../util/Embed');
const Webhook = require('../../util/Webhook');
const { youtube } = require('../../util/textParsing');

module.exports = {
  name: '', // The id of the youtube account
  /**
   * @async
   * @param {import("../../sources/base").sourceReturn} res
   * @param {import("../../config")} config
   * @return {Promise<import("../../sources/base").sourceReturn>}
   */
  execute: async ({ items, webhooks }, config) => {

  }
};